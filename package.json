{
  "name": "ai-release-risk-analyzer",
  "version": "0.1.0",
  "private": true,
  "description": "Hackathon MVP that turns GitHub PR data into a release risk recommendation.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
